# GoFind - 高性能并发文件搜索工具

[![Go Version](https://img.shields.io/badge/Go-1.21+-00ADD8?style=flat&logo=go)](https://go.dev/)
[![License](https://img.shields.io/badge/License-MIT-blue.svg)](LICENSE)
[![Go Report Card](https://goreportcard.com/badge/github.com/gofind/gofind)](https://goreportcard.com/report/github.com/gofind/gofind)

> **5秒搜索10万文件** | 并发搜索 | 正则匹配 | 优雅退出 | 超时控制

GoFind 是一个充分利用 Go 语言并发特性的命令行文件搜索工具，比传统 `grep` + `find` 组合快 3-5 倍，代码仅 300 行，是学习 Go 并发的完美示例项目。

---

## 一、项目背景与行业痛点

### 1.1 开发者日常困境

在大型代码库或日志目录中搜索文件内容，开发者常常面临：

| 痛点类型 | 具体表现 | 时间成本 |
|---------|---------|---------|
| 搜索慢 | 单线程遍历百万级文件 | 等待 30 秒以上 |
| 无法取消 | Ctrl+C 后进程仍在后台运行 | 资源浪费 |
| 功能分离 | `find` + `grep` 需要组合使用 | 学习成本高 |
| 无超时控制 | 卡在 NFS 挂载目录 | 永久等待 |
| 结果混乱 | 无并发安全的结果聚合 | 输出错乱 |

### 1.2 现有方案的局限性

| 工具 | 优点 | 缺点 |
|-----|------|------|
| `grep -r` | 原生支持，无需安装 | 单线程，无法利用多核 |
| `find + xargs + grep` | 可并行 | 命令复杂，参数难记 |
| `ag` (The Silver Searcher) | 速度快 | 不支持自定义并发数 |
| `ripgrep` (rg) | 性能优秀 | 代码量大，学习曲线陡 |

**核心缺失**：一个**代码简洁、并发模型清晰、体现 Go 特性**的教学级工具。

---

## 二、GoFind 解决方案

### 2.1 核心理念

> **让并发搜索变得简单优雅**

- **5 行代码**实现核心并发模型
- **3 个 goroutine 类型**覆盖生产者-消费者模式
- **1 个 context** 统一管理超时与取消

### 2.2 功能矩阵

| 功能模块 | 核心能力 | 技术实现 |
|---------|---------|---------|
| 目录遍历 | 并发扫描多级目录 | `filepath.WalkDir` + goroutine |
| 内容搜索 | 正则匹配文件内容 | `regexp` 包，支持 `-i` 忽略大小写 |
| 并发控制 | 可配置 Worker 数量 | 带缓冲 channel + `runtime.NumCPU()` |
| 超时控制 | 全局搜索超时 | `context.WithTimeout` |
| 优雅退出 | Ctrl+C 取消所有任务 | `context.WithCancel` + signal 监听 |
| 结果聚合 | 并发安全的结果收集 | `sync.Mutex` 或 channel |
| 进度统计 | 实时显示搜索进度 | `atomic` 计数器 |
| 文件过滤 | 忽略隐藏目录/文件 | `filepath.Match` 模式匹配 |

### 2.3 核心优势

#### 优势一：极致的性能提升，速度和效率的大幅提升

## 三、项目架构

### 3.1 Overview

gofind/
├── README.md                    # 项目文档（本文件）
├── go.mod                       # Go 模块依赖(已完成)
├── go.sum                       # 依赖校验（已完成）
├── Makefile                     # 构建脚本（已完成）
├── .golangci.yml                # 代码规范配置（已完成）
│
├── cmd/
│   └── gofind/
│       └── main.go              # 程序入口 (50 行)（已完成）
│
├── internal/
│   ├── engine/
│   │   ├── engine.go            # 核心引擎，并发协调 (120 行)(已完成)
│   │   ├── worker.go            # Worker 实现 (60 行)（已完成）
│   │   └── pipeline.go          # 流水线管理 (40 行)（已完成）
│   │
│   ├── searcher/
│   │   ├── searcher.go          # 搜索接口定义 (20 行)（已完成）
│   │   ├── regex.go             # 正则搜索实现 (50 行)（已完成）
│   │   └── text.go              # 纯文本搜索 (30 行)（已完成）
│   │
│   ├── walker/
│   │   └── walker.go            # 并发目录遍历 (80 行)（已完成）
│   │
│   ├── result/
│   │   ├── result.go            # 结果结构定义 (30 行)（已完成）
│   │   └── collector.go         # 结果收集器 (50 行)（已完成）
│   │
│   └── config/
│       └── config.go            # 配置解析与验证 (60 行)（已完成）
│
├── pkg/
│   ├── logger/
│   │   └── logger.go            # 统一日志接口 (40 行)（已完成）
│   │
│   └── errors/
│       └── errors.go            # 自定义错误类型 (30 行)（已完成）
│
├── tests/
│   ├── unit/
│   │   ├── engine_test.go       # 引擎单元测试（已完成）
│   │   ├── searcher_test.go     # 搜索器测试（已完成）
│   │   └── walker_test.go       # 遍历器测试（已完成）
│   │
│   ├── integration/
│   │   └── e2e_test.go          # 端到端测试（已完成）
│   │
│   └── benchmark/
│       ├── search_bench_test.go # 性能基准测试（已完成）
│       └── results.txt          # 测试结果（已完成）
│
├── scripts/
│   ├── build.sh                 # 构建脚本（已完成）
│   ├── test.sh                  # 测试脚本（已完成）
│   ├── benchmark.sh             # 性能测试脚本（已完成）
│   └── install.sh               # 安装脚本（已完成）
│
├── docs/
│   ├── architecture.md          # 架构设计文档（已完成）
│   ├── concurrency.md           # 并发模型详解（已完成）
│   ├── performance.md           # 性能优化指南（已完成）
│   └── examples.md              # 使用示例集（已完成）
│
└── .github/
    └── workflows/
        ├── ci.yml               # 持续集成（已完成）
        └── benchmark.yml        # 性能回归测试（已完成）

